# Tripways Figma reference

Open `overview.html` to review the current Tripways UI at Desktop 1440px and Mobile 375px.

The six images under `screenshots/` are intended for direct drag-and-drop into Figma as draft
reference frames.

`flights-from/SGN` currently renders a server error in the local runtime. Its two captures
document the current state rather than fabricating successful route data.

import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import ErrorPage from"./error";describe("public error boundary",()=>{it("hides dependency errors behind a useful recovery message",()=>{const html=renderToStaticMarkup(<ErrorPage error={new Error("ERR_PAGE_DATA_UNAVAILABLE")} reset={vi.fn()}/>);expect(html).toContain("Flight information is temporarily unavailable");expect(html).toContain("Try again");expect(html).not.toContain("ERR_PAGE_DATA_UNAVAILABLE")})});
